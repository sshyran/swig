/**
 * The newrelic module still checks for this file and blows up when its missing.
 * But all of the values for the config are now set in app.chassis via
 * environment variables.
 */
exports.config = {};
