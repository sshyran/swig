{
  "tasks": {
    "app": {
      "desc": "Runs an app",
      "flags": {
        "port": "Allows user to specify port to run on."
      },
      "eg": [
        "swig app",
        "swig app --port=8000"
      ]
    }
  }
}